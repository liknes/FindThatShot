<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!--
        Sidebar list styling (Tags filter, Cameras, etc.).

        Shares its row chrome with the folder TreeView in
        Resources/Components/FolderTree.xaml so the three rail panels
        (Folders / Tags / Cameras) read as one visual family — 12px text,
        4px-radius hover/select pill, App.Background.Hover hover,
        App.Accent.Subtle selection. Default WPF ListBoxItem is the wrong
        rhythm against the rest of the app (too tall, system-Highlight
        blue on selection); this rewires it to match.

        Application sites opt in via Style="{StaticResource App.ListBox}"
        rather than making this implicit, so the catalog grid
        ListBox (which has its own card-shaped App.VideoCard.ItemStyle)
        is unaffected.
    -->

    <Style x:Key="App.ListBoxItem" TargetType="ListBoxItem">
        <Setter Property="Padding" Value="8,5" />
        <Setter Property="Margin" Value="0,1" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Foreground" Value="{DynamicResource App.Foreground.Primary}" />
        <Setter Property="FontSize" Value="12" />
        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="OverridesDefaultStyle" Value="True" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ListBoxItem">
                    <Border x:Name="Bd"
                            Background="{TemplateBinding Background}"
                            CornerRadius="4"
                            Padding="{TemplateBinding Padding}"
                            SnapsToDevicePixels="True">
                        <ContentPresenter HorizontalAlignment="Stretch"
                                          VerticalAlignment="Center"
                                          TextElement.Foreground="{TemplateBinding Foreground}"
                                          TextElement.FontSize="{TemplateBinding FontSize}" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Bd" Property="Background" Value="{DynamicResource App.Background.Hover}" />
                        </Trigger>
                        <!-- Selection wins over hover. Matches the folder
                             tree's row treatment in FolderTree.xaml. -->
                        <Trigger Property="IsSelected" Value="True">
                            <Setter TargetName="Bd" Property="Background" Value="{DynamicResource App.Accent.Subtle}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="App.ListBox" TargetType="ListBox">
        <Setter Property="Background" Value="{DynamicResource App.Background.Inset}" />
        <Setter Property="BorderBrush" Value="{DynamicResource App.Border.Subtle}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Padding" Value="4" />
        <Setter Property="FontFamily" Value="{StaticResource App.FontFamily}" />
        <Setter Property="ItemContainerStyle" Value="{StaticResource App.ListBoxItem}" />
        <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled" />
        <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto" />
    </Style>

</ResourceDictionary>
